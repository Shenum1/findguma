/**
 * Placeholder for Supabase's generated database types. Once the project
 * exists, generate the real file with:
 *   npx supabase gen types typescript --project-id <id> > lib/supabase/types.ts
 * and add `Database` as the generic on createBrowserClient/createServerClient/
 * createClient in lib/supabase/{client,server,admin}.ts (a stand-in type here
 * — e.g. `Record<string, unknown>` — does not satisfy Supabase's generic
 * constraints and silently degrades every query's row type to `never`, so
 * the clients intentionally stay untyped-generic until the real file exists).
 */
export {};
